import { IconType } from "react-icons";

export interface IAboutServices {
  Title: string;
  About: string;
  Icon: IconType;
}

export interface ISkill {
  name: string;
  level: string;
  Icon: IconType;
}
